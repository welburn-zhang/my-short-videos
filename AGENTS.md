# AGENTS 总入口

本文件是本项目的唯一总入口。细则由对应 Skill 和知识库文档维护，避免在多处重复。

## 一、项目定位

**张半蛋**：8 年教育互联网从业者，面向抖音和小红书创作口播短视频。

**一句话定位**：教普通人用 AI 学得更快、学得更明白、学完真能用。

内容优先服务真实表达和长期积累，不为追热点、迎合观众或短期数据改变方向。

## 二、读取顺序

1. 项目讨论、决策、规划：先读根目录 `对话备忘录.md`。
2. 按任务调用对应 Skill；跨任务可组合使用。
3. 细则以 `.agents/skills/` 为准，`.cursor/rules/` 仅作 Cursor 兼容副本。
4. 当前要求与既有确认状态冲突时，先指出冲突并询问，不直接覆盖。
5. 形成新的长期策略、流程或明确结论时，提醒是否写入 `对话备忘录.md`。

| 任务 | Skill |
| --- | --- |
| 内容选题、脚本、标题、封面、发布复盘 | `$zbd-content-workflow` |
| 脚本初稿 | `$cp-script-draft` |
| 选题评估 | `$cp-topic-eval` |
| 内容诊断 | `$dbs-content` |
| 开头优化 | `$dbs-hook` |
| 标题核查 | `$dbs-xhs-title` |
| 去 AI 味 | `$humanizer-zh` |
| 分镜插画 | `$short-video-illustration-storyboard` |
| 数据分析、复盘、周报 | `$zbd-work-assistant` / `$data-weekly` |
| 学习讲解与测验 | `$zbd-learning-quiz` |
| 深度阅读 | `$zbd-deep-reading` |
| 项目备忘录 | `$zbd-session-memory` |

## 三、内容生产硬规则

1. **先判断再写**：封面文案、标题或完整脚本输出前，先用 `$dbs-content`；开头不稳再用 `$dbs-hook`，标题封面用 `$dbs-xhs-title`。先给判断，用户确认后再写正文。
2. **先学表达再去 AI 味**：读取 `$zbd-content-workflow` 的“表达习惯”，再选最近 3 份已确认或已发布脚本提取稳定口语习惯；内部初稿完成后调用 `$humanizer-zh`，只输出处理后的版本。
3. **脚本固定五部分**：发布信息、封面/标题/标签、3 个开头候选、拍摄版正文、分镜与资产清单。
4. **不设统一时长**：按内容需要控制，以讲清楚且不注水为准。
5. **统一标题封面**：默认只出一个双平台通用版本；用户明确要求时才拆平台版本。
6. **固定收口**：默认使用“好了，记得点赞收藏+关注，后面我会分享更多适合小白观看的AI内容，我们下期再见，拜拜。”
7. **分镜先确认**：逐项确认分镜与资产清单后，只为“待生图”项目生成插画；资产全部完成验收后才能剪辑。
8. **数据降权**：每周或双周整体复盘，重点看表达是否被理解、开头是否留住、评论是否有真实共鸣。
9. **家庭教育暂缓**：等教育标签稳定、家长自然聚集后再启动。

表达固定偏好：不用“过几天就散了/散掉了”，改用“当下会觉得懂了，过几天基本就忘了”。

## 四、平台与视觉规则

- AI 工具、Agent、工作流内容从真实任务和结果切入，不以上来解释概念、产品功能或“今天我们来讲”开场。
- AI 工具类开头参考 `素材库/参考视频/秋芝2046/`：直接切入 → 强判断/强结果 → 具体画面或生活化比喻 → 15 秒内说明能解决什么。
- AI 工具教程避免集中出现“官网、下载、登录、ChatGPT、OpenAI、Google、GitHub、浏览器插件、外网服务”等表达；发布前检查标题、封面、标签和前 30 秒。
- 抖音避免在标题、封面、标签、前 10 秒密集叠加“裁员、被裁、AI 替代、公司在砍”等词，默认不用 `#裁员`。
- 封面先读 `素材库/短视频封面制作规则.md`，再参考 `素材库/人物形象/张半蛋/`。
- 封面使用 Codex 内置 `imagegen` 原生生成 `3:4` 和 `4:3` 两版；文字必须由生图模型生成，禁止 PIL、Canvas、HTML、SVG 或固定模板后期排字。
- 关键词必须与背景保持高对比，并按手机缩略图检查；生成完成后主动询问是否调用 `$img-compress`。

## 五、内容工作流

```text
灵感速记
→ 选题评估
→ dbs 判断并确认方向
→ 学习近期表达
→ 内部初稿 + humanizer
→ 输出五部分完整脚本
→ 确认封面文案和主标题
→ 生成两版封面并询问压缩
→ 逐项确认分镜与资产
→ 制作缺失资产并验收
→ 剪辑、发布、复盘、提取素材
```

脚本框架：

| 类型 | 路径 |
| --- | --- |
| 深度价值型 | `脚本模板库/深度价值型.md` |
| 干货清单型 | `脚本模板库/干货清单型.md` |
| 对话冲突型 | `脚本模板库/对话冲突型.md` |

常用入口：

| 内容 | 路径 |
| --- | --- |
| 灵感 | `知识库/灵感速记.md` |
| 选题池 | `知识库/选题池.md` |
| 账号方向 | `知识库/账号定位与方向规划.md` |
| 内容要求 | `知识库/内容创作要求.md` |
| 制作流程 | `知识库/短视频制作流程规范.md` |
| 封面规则 | `素材库/短视频封面制作规则.md` |
| 钩子、案例、Prompt | `素材库/钩子公式库.md`、`素材库/金句&案例库.md`、`素材库/Prompt模板库.md` |

## 六、操作与 Git

- 每日雷达优先使用 `office` Browser Bridge 的真实抖音、小红书结果；小红书登录墙先登录再重试，Bridge 断开先检查 daemon 和正式安装的扩展。
- 每日雷达指定群为“每日短视频热点选题雷达”；其他群必须先确认。企微发送前清理代理环境，严禁输出 webhook、token、cookie。
- API token、密钥不得写入回复、日志、文档、长期记忆或项目文件。
- Git 提交脚本、规则、配置和最终交付图片；图片被忽略时使用 `git add -f`。不提交视频、字体、压缩包、安装包、数据导出、依赖和构建产物。
- 单个二进制文件超过 10 MB 时先确认是否使用 Git LFS。
- 远端使用 `git@github-welburnzhang:welburn-zhang/my-short-videos.git`。
