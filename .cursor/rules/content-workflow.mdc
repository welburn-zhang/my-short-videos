---
description: 短视频项目的内容策略与脚本协作规则
alwaysApply: true
---

# Cursor 兼容规则

`AGENTS.md` 是项目唯一总入口，本文件只保留 Cursor 必需的摘要；冲突时以 `AGENTS.md` 和 `.agents/skills/` 为准。

## 内容判断

- 优先真实表达、创作者适配和表达清晰度，数据只作周复盘或双周复盘参考。
- AI 内容绑定真实任务，展示问题、修改过程、结果对比和人工判断，不做纯功能介绍。
- 家庭教育暂缓；不为短期数据、热点或爆款模板改变方向。

## 脚本流程

1. 封面、标题和脚本前先做 `/dbs-content` 核查；开头用 `/dbs-hook`，标题用 `/dbs-xhs-title`。先确认方向，再写正文。
2. 读取 `.agents/skills/zbd-content-workflow/SKILL.md` 的“表达习惯”，选最近 3 份已确认或已发布脚本学习稳定口语，再调用去 AI 味 Skill。
3. 默认输出一个双平台通用的封面文案、主标题和标签。
4. 完整脚本固定五部分：发布信息、封面/标题/标签、3 个开头候选、拍摄版正文、分镜与资产清单。
5. 不设统一时长，以讲清楚且不注水为准。
6. 固定收口：“好了，记得点赞收藏+关注，后面我会分享更多适合小白观看的AI内容，我们下期再见，拜拜。”
7. 分镜逐项确认后只制作“待生图”资产；全部资产验收后才能剪辑。

## 封面与审核

- 封面先读 `素材库/短视频封面制作规则.md`，参考本人照片，用 Codex 内置 `imagegen` 原生生成 `3:4` 和 `4:3` 两版。
- 禁止 PIL、Canvas、HTML、SVG 或固定模板后期排字；生成后询问是否压缩。
- AI 工具内容不做下载、安装、登录和外网服务教学，优先讲真实任务和结果。
- 抖音标题、封面、标签和前 10 秒避免密集叠加就业焦虑词。
