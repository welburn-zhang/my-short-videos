# Scene 04｜1:05-1:22 不是这次做什么

这一段解释 AGENTS.md 的作用：不是告诉 AI 这一次做什么，而是告诉它每次进入项目前默认守什么规矩。

## 规格

| 项目 | 规格 |
| --- | --- |
| 时长 | 17 秒 |
| 网格 | 4x4，共 16 格 |
| 比例 | 横屏 16:9 |

## 分镜节奏

1-4：左侧真人占位，右侧出现 `AGENTS.md 干的就是这件事`。  
5-8：对比卡左栏 `这次做什么` 打叉，右栏 `默认守什么规矩` 打勾。  
9-12：左栏变成一次性便签，右栏变成门口长期规则牌。  
13-16：总结卡 `AGENTS.md = 项目默认规则`。

## 给 image2 / Image Gen 的提示词

Create a single 4x4 storyboard sheet for Scene 04, 17 seconds, horizontal 16:9. Same style as Scene 01: warm yellow, cream white cards, orange outlines, dark gray bold Chinese typography, simple left presenter placeholder, right-side info graphics.

Scene summary: AGENTS.md is not about this one task; it is the default rule that the AI should follow before entering the project.

Panels: 1-4 left presenter placeholder + right document card “AGENTS.md”; 5-8 comparison card, left “这次做什么” with X, right “默认守什么规矩” with check mark; 9-12 sticky note on left fades, permanent rule paper on door becomes stronger; 13-16 final card “AGENTS.md = 项目默认规则”.

Use 4x4 grid, each panel 16:9, black number and duration labels. Keep layout clean, no realistic person, no clutter, no blue-purple tones.
